apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: root-cert
  namespace: envoy-system
spec:
  secretName: root-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - "mydevopsjourney.fun"
