apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: frontend
  namespace: realworld-app
spec:
  interval: 10m
  chart:
    spec:
      chart: ./k8s/charts/realworld-app
      version: "0.2.4"
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      interval: 10m
  values:
    app:
      name: frontend
    replicaCount: 1
    deployment:
      image:
        repository: ghcr.io/nospooksallowed/realworld-fullstack-app
        tag: frontend-af154d26f436e341a56de6840156b5e8939d1a81-94 # {"$imagepolicy": "flux-system:frontend-policy:tag"}
      port: 80
      targetPort: 8080
